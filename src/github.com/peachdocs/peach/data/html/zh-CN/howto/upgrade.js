document.write("<h1>更新版本</h1>\n\n<p>如果您之前是通过源码安装的 Peach，则可以直接通过执行以下命令来完成 Peach 的二进制升级操作：</p>\n<pre><code class=\"language-sh\">$ go get -u github.com/peachdocs/peach\n</code></pre>\n\n<p>然后在每个项目的目录下，都要按照以下步骤完成升级操作：</p>\n\n<ol>\n<li>移除现有的默认 <code>conf</code>、<code>public</code> 和 <code>templates</code> 目录</li>\n<li>重新拷贝这些目录到当前项目中</li>\n<li>检查变更日志，确保没有兼容性问题</li>\n<li>在自定义目录下的 <code>templates</code> 和 <code>public</code> 目录做出任何需要的修改以修复可能的兼容性问题</li>\n</ol>\n")