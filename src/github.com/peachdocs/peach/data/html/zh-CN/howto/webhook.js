document.write("<h1>使用 Web 钩子同步文档</h1>\n\n<p>正常人都不希望自己每次更新文档的时候都要远程登录服务器去进行手动更新操作，特别是进行一些小修改的时候。</p>\n\n<p>为此，Peach 提供了通过 Web 钩子来实时同步文档的功能。</p>\n\n<p>那么我们要做的第一件事情就是确保通过 <a href=\"documentation#git-%E4%BB%93%E5%BA%93\">Git 仓库</a> 来 <a href=\"documentation\">创建文档仓库</a>。</p>\n\n<p>然后就可以在 <code>custom/app.ini</code> 文件中填写密钥值：</p>\n<pre><code class=\"language-ini\">[docs]\nSECRET = mysecret\n</code></pre>\n\n<p>触发钩子的格式和 URL 路径为 <code>POST /hook?secret={secret}</code>，其中密钥值 <code>secret</code> 是通过 URL 查询的方式传递的。</p>\n\n<p>必须注意的是密钥值 <code>secret</code> 只有在和 <code>SECRET</code> 的值 <strong>完全匹配</strong> 的情况下才视为有效。因此，如果您的 <code>SECRET</code> 值为空，则 URL 查询传递的密钥值 <code>secret</code> 也必须为空，反之亦然。</p>\n\n<h2>GitHub</h2>\n\n<p>我们在这里阐述一下如何在 GitHub 上设置 Peach 的 Web 钩子，但在其它地方也是基本一样的。</p>\n\n<ol>\n<li>访问文档仓库的新增 Web 钩子页面（<code>/:username/:reponame/settings/hooks/new</code>）</li>\n<li>填写 <strong>Payload URL</strong> 值为触发钩子的 URL 路径，例如：<code>http://peachdocs.org/hook?secret=mypeach</code></li>\n<li>单击 <code>Add webhook</code> 搞定。 :tada:</li>\n</ol>\n\n<p><img src=\"/docs/images/github_webhook.png\" alt=\"\" /></p>\n\n<p>从此以后，凡是在 GitHub 推送或合并请求，您的文档都会自动更新到最新版本。</p>\n")