document.write("<h1>添加静态资源</h1>\n\n<p>自定义静态资源的目录同样需要被放置在 <code>custom</code> 目录中，并且与默认的静态资源目录具有相同的目录结构：</p>\n<pre><code class=\"language-sh\">$ tree custom/public\ncustom/public\n├── css\n├── fonts\n├── img\n└── js\n</code></pre>\n\n<h2>Logo</h2>\n\n<p>自定义站点的第一步就是将默认的 Logo 替换为您项目的 Logo。</p>\n\n<p>因此，您需要将 Logo 文件重命名为 <code>favicon.ico</code> 然后放置到 <code>custom/public/img</code> 目录中：</p>\n<pre><code class=\"language-sh\">$ tree custom/public/img\ncustom/public/img\n└── favicon.ico\n</code></pre>\n\n<p>无论 Logo 文件的格式是什么（PNG、JPEG 或其它），都必须遵守该名称约定，浏览器会自动识别并展示图片。</p>\n\n<p>:speech_balloon: 您需要强制刷新浏览器以查看最终效果。</p>\n\n<h2>自定义 CSS</h2>\n\n<p>根据约定，您可以在 <code>custom/public/css</code> 目录中创建一个名为 <code>my.css</code> 的 CSS 文件：</p>\n<pre><code class=\"language-sh\">$ tree custom/public/css\ncustom/public/css\n└── my.css\n</code></pre>\n\n<p>然后在 <code>custom/app.ini</code> 文件中修改相关配置：</p>\n<pre><code class=\"language-ini\">[asset]\nCUSTOM_CSS = /css/my.css\n</code></pre>\n\n<h2>自定义 UI 框架</h2>\n\n<p>如果您需要完全自定义一个其它的 UI 框架，则可以将所有需要的静态资源都放置到 <code>custom/public</code> 目录中，然后对 <code>base.html</code> 模板文件中的旧链接进行替换：</p>\n<pre><code class=\"language-html\">...\n    &lt;!--&lt;link href=&quot;/css/semantic.min.css?v={{AppVer}}&quot; rel=&quot;stylesheet&quot; /&gt;--&gt;\n    &lt;!--&lt;link href=&quot;/css/peach.css?v={{AppVer}}&quot; rel=&quot;stylesheet&quot; /&gt;--&gt;\n    &lt;link href=&quot;/css/bootstrap.min.css&quot; rel=&quot;stylesheet&quot; /&gt;\n    \n    &lt;!--&lt;script type=&quot;text/javascript&quot; src=&quot;/js/semantic.min.js?v={{AppVer}}&quot;&gt;&lt;/script&gt;--&gt;\n    &lt;!--&lt;script type=&quot;text/javascript&quot; src=&quot;/js/peach.js?v={{AppVer}}&quot;&gt;&lt;/script&gt;--&gt;\n    &lt;script type=&quot;text/javascript&quot; src=&quot;/js/my.js&quot;&gt;&lt;/script&gt;\n...\n</code></pre>\n")