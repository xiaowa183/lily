document.write("<h1>添加单独页面</h1>\n\n<p>我们有时候都会有显示单独的页面来作为网站的一部分，而不是文档的一部分的需求。因此，Peach 也支持自定义某些文档为单独页面。</p>\n\n<p>在 <code>TOC.ini</code> 文件中，添加一个名为 <code>[pages]</code> 的分区：</p>\n<pre><code class=\"language-ini\">[pages]\n-: donate\n</code></pre>\n\n<p>然后，您就可以将名称为 <code>donate.md</code> 的文件放置在每一个支持语言的目录下。</p>\n\n<p>例如，所支持的语言为 <code>en-US</code> 和 <code>zh-CN</code>，那么您应该拥有以下两个文件：</p>\n\n<ul>\n<li><code>/en-US/donate.md</code></li>\n<li><code>/zh-CN/donate.md</code></li>\n</ul>\n\n<p>但是，<strong>默认语言规则</strong> 同样适用于单独页面，如果某个页面没有用户当前浏览语言的版本，则会自动显示默认语言的版本。</p>\n\n<p>访问该页面的 URL 路径为 <code>/donate</code>，即与不包含 <code>.md</code> 后缀的文件名称相同。</p>\n\n<p>最后要说明的是，您可以在 <code>TOC.ini</code> 文件中定义任意多个单独页面。</p>\n")