document.write("<h1>使用扩展功能</h1>\n\n<h2>搜索</h2>\n\n<p>搜索扩展处于默认开启状态，修改以下配置可以禁用该扩展：</p>\n<pre><code class=\"language-ini\">[extension]\nENABLE_SEARCH = false\n</code></pre>\n\n<h2>Highlight JS</h2>\n\n<p>Highlight JS 默认启用且不可关闭，但可以自定义它的渲染主题。</p>\n\n<p>例如，您希望将默认主题替换为 <code>zenburn</code>，则需要先将 <code>zenburn.css</code> 文件拷贝至 <code>custom/public/css</code> 目录下，然后在 <code>custom/app.ini</code> 文件中做出如下修改：</p>\n<pre><code class=\"language-ini\">[extension]\nHIGHLIGHTJS_CUSTOM_CSS = /css/zenburn.css\n</code></pre>\n\n<h2>编辑页面</h2>\n\n<p>您可以在文档页面添加一个编辑页面的链接以方便用户一同协助改进文档。</p>\n\n<p>您需要对配置文件做出相应的修改才能使用该扩展：</p>\n<pre><code class=\"language-ini\">[extension]\nENABLE_EDIT_PAGE = true\nEDIT_PAGE_LINK_FORMAT = https://github.com/peachdocs/docs/blob/master/{lang}/{blob}\n</code></pre>\n\n<p>请不要忘记将链接修改为您的仓库链接！</p>\n\n<p>:white_flower: 如果您正在使用低于 <strong>0.7.0</strong> 版本的 Peach，并且使用了自定义模板，则需要在 <code>docs.html</code> 模板文件的 <code>{{Content|safe}}</code> 这一行之前加入以下内容：</p>\n<pre><code class=\"language-django\">{% if Extension.EnableEditPage %}\n    &lt;a class=&quot;ui blue button&quot; id=&quot;edit_page&quot; href=&quot;{{EditPageLink}}&quot;&gt;{{Tr(Lang, &quot;docs.edit_page&quot;)}}&lt;/a&gt;\n{% endif %}\n</code></pre>\n\n<h2>Disqus</h2>\n\n<p>您需要在 <a href=\"https://disqus.com/\">Disqus</a> 上添加一个站点后才能使用 Disqus 扩展，然后做出相应的配置修改：</p>\n<pre><code class=\"language-ini\">[extension]\nENABLE_DISQUS = true\nDISQUS_SHORT_NAME = mypeach\n</code></pre>\n\n<h2>多说</h2>\n\n<p>:white_flower: 要求 Peach 版本不得低于 <strong>0.7.1</strong></p>\n\n<p>您需要在 <a href=\"http://duoshuo.com/\">DuoShuo</a> 上添加一个站点后才能使用多说扩展，然后做出相应的配置修改：</p>\n<pre><code class=\"language-ini\">[extension]\nENABLE_DUOSHUO = true\nDUOSHUO_SHORT_NAME = mypeach\n</code></pre>\n\n<h2>Google Analytics</h2>\n\n<p>您需要在 <a href=\"http://www.google.com/analytics/\">Google Analytics</a> 上创建一份档案后才能使用 Google Analytics 扩展，然后做出相应的配置修改：</p>\n<pre><code class=\"language-ini\">GA_BLOCK = &quot;&quot;&quot;&lt;script&gt;\n  (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){\n  (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),\n  m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)\n  })(window,document,'script','//www.google-analytics.com/analytics.js','ga');\n  ga('create', 'UA-xxxxxxxx-y', 'auto');\n  ga('send', 'pageview');\n&lt;/script&gt;&quot;&quot;&quot;\n</code></pre>\n\n<hr />\n\n<p>由于您对模板文件拥有完全的自定义能力，因此总是可以通过修改相应的 <code>.html</code> 模板文件来达到使用或添加更多扩展的需求。</p>\n")