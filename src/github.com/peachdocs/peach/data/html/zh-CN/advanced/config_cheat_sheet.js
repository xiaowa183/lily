document.write("<h1>配置文件完全手册</h1>\n\n<p>在阅读本文档之前，您必须确保您已经了解所有的改动都必须发生在 <code>custom/app.ini</code> 文件中，并且使用 <a href=\"https://en.wikipedia.org/wiki/INI_file\">INI</a> 语法。</p>\n\n<p>另外，该文档将只描述其它页面没有提及到的配置选项。</p>\n\n<h2>站点配置</h2>\n<pre><code class=\"language-ini\">[site]\n# Peach 实例的名称\nNAME = Peach Server\n# HTML 描述\nDESC = Peach is a web server for multi-language, real-time update and searchable documentation.\n# 使用 CDN 来加载静态资源，但 Google Font 将总是从 CDN 加载\nUSE_CDN = true\n# 外部访问站点的 URL\nURL = http://localhost:5555\n</code></pre>\n\n<h2>页面配置</h2>\n<pre><code class=\"language-ini\">[page]\n# 设为 false 表示站点没有首页，用户将总是被自动重定向到文档页面\nHAS_LANDING_PAGE = true\n</code></pre>\n")