document.write("<h1>Add Static Resources</h1>\n\n<p>The directory of custom static resources follows the same structure of default ones, and should be put under <code>custom</code> directory as well:</p>\n<pre><code class=\"language-sh\">$ tree custom/public\ncustom/public\n├── css\n├── fonts\n├── img\n└── js\n</code></pre>\n\n<h2>Logo</h2>\n\n<p>The very first thing you want to customize is the Logo of your project.</p>\n\n<p>To do that, rename your Logo file to <code>favicon.ico</code> and put it under <code>custom/public/img</code> directory:</p>\n<pre><code class=\"language-sh\">$ tree custom/public/img\ncustom/public/img\n└── favicon.ico\n</code></pre>\n\n<p>It does not matter what your Logo image format is (PNG, JPEG or whatever), it&rsquo;s just a name convention, and let browser figures it.</p>\n\n<p>:speech_balloon: You need do a hard refresh on your browser to actually see the effect.</p>\n\n<h2>Custom CSS</h2>\n\n<p>By convention, you can create a CSS file named <code>my.css</code> under <code>custom/public/css</code> directory:</p>\n<pre><code class=\"language-sh\">$ tree custom/public/css\ncustom/public/css\n└── my.css\n</code></pre>\n\n<p>And then, change settings on your configuration file <code>custom/app.ini</code>:</p>\n<pre><code class=\"language-ini\">[asset]\nCUSTOM_CSS = /css/my.css\n</code></pre>\n\n<h2>Custom UI Framework</h2>\n\n<p>In the case you need to completely change a UI framework, you can put all needed static resources on <code>custom/public</code> directory and change template file <code>base.html</code> to replace old resource links with custom ones:</p>\n<pre><code class=\"language-html\">...\n    &lt;!--&lt;link href=&quot;/css/semantic.min.css?v={{AppVer}}&quot; rel=&quot;stylesheet&quot; /&gt;--&gt;\n    &lt;!--&lt;link href=&quot;/css/peach.css?v={{AppVer}}&quot; rel=&quot;stylesheet&quot; /&gt;--&gt;\n    &lt;link href=&quot;/css/bootstrap.min.css&quot; rel=&quot;stylesheet&quot; /&gt;\n    \n    &lt;!--&lt;script type=&quot;text/javascript&quot; src=&quot;/js/semantic.min.js?v={{AppVer}}&quot;&gt;&lt;/script&gt;--&gt;\n    &lt;!--&lt;script type=&quot;text/javascript&quot; src=&quot;/js/peach.js?v={{AppVer}}&quot;&gt;&lt;/script&gt;--&gt;\n    &lt;script type=&quot;text/javascript&quot; src=&quot;/js/my.js&quot;&gt;&lt;/script&gt;\n...\n</code></pre>\n")