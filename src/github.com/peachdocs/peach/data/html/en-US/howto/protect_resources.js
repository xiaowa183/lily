document.write("<h1>Protect Internal Resources</h1>\n\n<p>:white_flower: Requires Peach version <strong>0.9.0</strong></p>\n\n<p>When some documents are internal use only, you can enable protect mode for them.</p>\n\n<p>What does protect mode do?</p>\n\n<ol>\n<li>Authentication based on HTTP Basic Authorization.</li>\n<li>User defines a list of authorized users along with their password.</li>\n<li>User defines a list of resources to be protected and individuals who can access.</li>\n</ol>\n\n<p>To enable protect mode, you would need to create a file named <code>protect.ini</code> and put it in same directory as your <code>TOC.ini</code> does.</p>\n\n<p>Here is an example:</p>\n<pre><code class=\"language-ini\">[user]\nuser1 = 5F4DCC3B5AA765D61D8327DEB882CF99\nuser2 = xxx\nuser3 = xxx\n\n[auth]\nhowto/documentation = user1,user2,user3\nhowto/webhook = user1,user2\nhowto/templates = user1\n</code></pre>\n\n<p>User password must be MD5-encoded, and use comma (<code>,</code>) to seperate multiple users.</p>\n\n<p>Document URLs do not need <code>/docs/</code> prefix as you can tell.</p>\n\n<p>Based on example file, there are three users are authorized, and three resources are protected:</p>\n\n<ul>\n<li><code>user1</code> has all access to three resources.</li>\n<li><code>user2</code> can only access <code>howto/documentation</code> and <code>howto/webhook</code>, and gets <strong>403</strong> when tries to access <code>howto/templates</code>.</li>\n<li><code>user3</code> can only access <code>howto/documentation</code>, and gets <strong>403</strong> when tries to access <code>howto/webhook</code> and <code>howto/templates</code>.</li>\n<li>Fail to authenticate will result in <strong>401</strong>.</li>\n</ul>\n")