document.write("<h1>Upgrade Peach</h1>\n\n<p>If you installed Peach from source code, you can then simply execute following command to upgrade Peach binary:</p>\n<pre><code class=\"language-sh\">$ go get -u github.com/peachdocs/peach\n</code></pre>\n\n<p>Then in all of your project directories, you should do following steps for each of them:</p>\n\n<ol>\n<li>Remove current default <code>conf</code>, <code>public</code> and <code>templates</code> directories.</li>\n<li>Copy over latest version of those directories.</li>\n<li>Check with change log, make sure there is no compatibility issues.</li>\n<li>Make necessary changes in your custom <code>templates</code> and <code>public</code> directories to fix any possible compatibility issues.</li>\n</ol>\n")