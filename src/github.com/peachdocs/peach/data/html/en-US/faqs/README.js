document.write("<h1>FAQs</h1>\n\n<h3>How do I use reverse proxy with NGINX?</h3>\n\n<p>Here is an example of NGINX config section, but values can be different from your situation:</p>\n<pre><code class=\"language-nginx\">server {\n    listen 80;\n    server_name peachdocs.org;\n    \n    location / {\n        proxy_pass http://localhost:5556;\n    }\n}\n</code></pre>\n\n<h3>Does my custom directory have to be a Git repository?</h3>\n\n<p>No, your custom directory does not have to be a Git repository, it&rsquo;s just a best practice to sync and backup your custom things.</p>\n\n<h3>How do I setup single language documentation?</h3>\n\n<p>By default, Peach requires you have two languages version of documentation (<code>en-US</code> and <code>zh-CN</code>), otherwise it will complain an error of documentation missing. To use single language documentation, you need to make similar changes in your <code>custom/app.ini</code> as follows:</p>\n<pre><code>[i18n]\nLANGS = en-US\nNAMES = English\n</code></pre>\n")