document.write("<h1>Getting Started</h1>\n\n<p>Let&rsquo;s create our first Peach project!</p>\n\n<p>By convention, every Peach project should be named in format <code>*.peach</code>, here we choose <code>my.peach</code>.</p>\n\n<p>In this tutorial, we&rsquo;re going to put our Peach project in download directory:</p>\n<pre><code class=\"language-sh\">$ cd ~/Downloads\n</code></pre>\n\n<p>Then, we need to setup default things (Suppose <code>$GOPATH/bin</code> has been added to your <code>$PATH</code>):</p>\n<pre><code class=\"language-sh\">$ peach new -target=my.peach\n➜  Creating 'my.peach'...\n➜  Creating 'templates'...\n➜  Creating 'public'...\nDo you want to use custom templates?[Y/n] n\n✓  Done!\n</code></pre>\n\n<p>Let&rsquo;s take a quick look of how our project is organized:</p>\n<pre><code class=\"language-sh\">$ cd my.peach\n$ tree -L 2\n.\n├── conf\n│   ├── app.ini\n│   └── locale\n├── public\n│   ├── css\n│   ├── fonts\n│   ├── img\n│   └── js\n└── templates\n    ├── 404.html\n    ├── base.html\n    ├── disqus.html\n    ├── docs.html\n    ├── duoshuo.html\n    ├── footer.html\n    ├── home.html\n    ├── navbar.html\n    └── search.html\n</code></pre>\n\n<p>Great! We&rsquo;ve got almost everything we need, except one thing: custom configuration.</p>\n\n<p>To quickly get started with, we are going to use the one Peach uses, it is <a href=\"https://github.com/peachdocs/peach.peach\">open sourced on GitHub</a>. :heart_eyes:</p>\n\n<p>So&hellip; how do we use that?</p>\n\n<p>Right, we clone it to local system, and name the directory to <code>custom</code> which Peach uses it to load all custom things.</p>\n<pre><code class=\"language-sh\">git clone https://github.com/peachdocs/peach.peach.git custom\n</code></pre>\n\n<p>Very well! You can&rsquo;t wait to try it out, can you?</p>\n\n<p>Why don&rsquo;t you now start a Peach server in current directory (in this case, it is <code>~/Downloads/my.peach</code>):</p>\n<pre><code class=\"language-sh\">$ peach web\n</code></pre>\n\n<p>OK, Peach is now running and you should see log on your terminal:</p>\n<pre><code>[Peach] 15-10-06 19:58:44 [ INFO] Peach 0.8.0.1025\n[Peach] 15-10-06 19:58:44 [ INFO] Peach Server Listen on 0.0.0.0:5556\n</code></pre>\n\n<p>Based on the log, Peach is customized itself to listen on <code>0.0.0.0:5556</code> (default is <code>5555</code>), and if you open your browser and visit <a href=\"http://localhost:5556/\">http://localhost:5556/</a>, miracle happens!</p>\n\n<p>If you curious about what is in Peach&rsquo;s custom configuration, let me briefly explain in the <code>custom/app.ini</code> file:</p>\n<pre><code class=\"language-ini\"># Change listen port\nHTTP_PORT = 5556\n\n[docs]\n# Set documentation to a remote Git source adrress\nTYPE = remote\n# URL to remote Git source\nTARGET = https://github.com/Unknwon/peach-docs.git\n</code></pre>\n\n<p>This should explain why we didn&rsquo;t do anything with documentation but we can view it automatically.</p>\n\n<p>Well, let&rsquo;s moving forward: <a href=\"../howto/documentation\">Setup your documentation repository</a>.</p>\n")